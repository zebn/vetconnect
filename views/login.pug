extends layout
block content
    div.container
      .row.justify-content-center
        .col-md-6
            h1.my-5.text-center Iniciar sesión
            form(action='/login' method='post')
                .mb-3.row.align-items-center
                    label.form-label.col-sm-4(for='username') Email: 
                    .col-sm-8
                        input#username.form-control(type='email' name='username' maxlength="45" required)
                .mb-3.row.align-items-center
                    label.form-label.col-sm-4(for='current-password') Contraseña: 
                    .col-sm-8
                        input#current-password.form-control(type='password' name='current-password' required)
                .mb-3.row.align-items-center
                    label.form-label.col-sm-4(for='rememberme') Guardar contraseña: 
                    .col-sm-8
                        input#rememberme(name='rememberme'  type='checkbox' value="yes")
                .mb-3.row.align-items-center
                    .col-sm-8.offset-sm-4
                        button.btn.btn-success.px-4.py-2.ms-5(type='submit') Entrar

    //- h1 Sign in
    //- form(action='/login' method='post')
    //-     section
    //-         label(for='username') Username
    //-         input#username(name='username' type='text' autocomplete='username' required='' autofocus='')
    //-     section
    //-         label(for='current-password') Password
    //-         input#current-password(name='password' type='password' autocomplete='current-password' required='')
    //-     section
    //-         label(for='rememberme') Remember me
    //-         input#rememberme(name='rememberme'  type='checkbox' value="yes")
    //-     button(type='submit') Sign in
